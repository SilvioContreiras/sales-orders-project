export function App() {
  return (
    <main className="flex min-h-full flex-col items-center justify-center gap-3 p-8 text-center">
      <span className="rounded-full bg-brand-100 px-3 py-1 text-sm font-medium text-brand-700">
        OVGS
      </span>
      <h1 className="text-3xl font-bold text-slate-900">Sales Order Management System</h1>
      <p className="max-w-md text-slate-600">
        Project scaffolding is ready. Core infrastructure (router, store, mocked API) comes next.
      </p>
    </main>
  );
}
